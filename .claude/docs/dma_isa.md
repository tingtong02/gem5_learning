# DMA 模块宏指令 ISA (512-bit)

## 1. 架构假设与设计前提

本 ISA 设计基于以下底层存储与总线架构的物理约定：

1. **物理地址空间划分 (32-bit)**:
* **DRAM (主存)**: 1 GB 容量，基地址挂载于 `0x2000_0000`，范围 `0x2000_0000` ~ `0x5FFF_FFFF`。
* **SPM (Scratchpad Memory)**: 256 MB 容量，基地址挂载于 `0x6000_0000`，范围 `0x6000_0000` ~ `0x6FFF_FFFF`。


2. **地址映射机制**:
* 采用虚拟地址到物理地址的**恒等映射 (Identity Mapping)**，即 VA = PA。
* 基于以上两点，系统最大有效物理地址不会超过 `0x6FFF_FFFF`，因此**地址字段被安全压缩至 32-bit**，从而释放大量总线指令空间。


3. **数据位宽约束**:
* 底层 DMA 访存位宽按 **64 Bytes** 对齐，需结合 `data_type` 计算 Burst 传输的元素个数。



---

## 2. 宏指令位段分配表 (Total: 512 bits)

为了方便硬件解码器 (SystemVerilog) 截取和软件端 (Gem5 C++ 模型) 解析，除公共头部外，DMA 专属控制字段全部严格对齐到 **32-bit (Word) 边界**。

| 内存字边界 | 字段名称 (Field) | 位偏移 (Bits) | 位宽 | 字段含义与硬件动作解释 |
| :--- | :--- | :--- | :--- | :--- |
| **Word 15** | `Common_Header` | `<511:480>` | 32 | **公共头部** ：<br>• `device_type`<511:508> = `4'b0100` (DMA) <br>• `device_id`<507:504>: 硬件实例 ID <br>• `op_code`<503:496> 被 DMA 重定义为 3 部分：<br>  - `[7:5] data_type`: `000`(INT8), 其他保留<br>  - `[4:2] xfer_mode`: `000`(D->S), `001`(S->D), `010`(S->S), `011`(D->D), 其他保留用于未来拓展<br>  - `[1:0]`: 保留位<br>• `sync_indicator`<495:488>: 同步的信号量 <br>• `set_indicator_sns`<487:487>: 指令执行完成后拉高 src tile 信号量（但是暂不涉及多tile协同，即目前开发无需关注） <br>• `set_indicator_snd`<486:486>: 指令执行完成后拉高 dst tile 信号量（但是暂不涉及多tile协同，即目前开发无需关注） <br>• `<485:480>`: 保留字段  |
| **Word 14** | `src_base_addr` | `<479:448>` | 32 | **源起始物理地址**。支持指针直传。<br>*(范围校验: `0x20000000`~`0x6FFFFFFF`)* |
| **Word 13** | `dst_base_addr` | `<447:416>` | 32 | **目的起始物理地址**。 |
| **Word 12** | `tensor_shape_h` | `<415:384>` | 32 | 三维张量逻辑高度 (**H 轴长度**)。 |
| **Word 11** | `tensor_shape_w` | `<383:352>` | 32 | 三维张量逻辑宽度 (**W 轴长度**)。 |
| **Word 10** | `tensor_shape_c` | `<351:320>` | 32 | 三维张量逻辑通道数 (**C 轴长度**)。 |
| **Word 9** | `src_stride_h` | `<319:288>` | 32 | 源数据在 **H** 轴的物理跨度 (Stride)。 |
| **Word 8** | `src_stride_w` | `<287:256>` | 32 | 源数据在 **W** 轴的物理跨度 (Stride)。 |
| **Word 7** | `src_stride_c` | `<255:224>` | 32 | 源数据在 **C** 轴的物理跨度 (Stride)。 |
| **Word 6** | `dst_stride_h` | `<223:192>` | 32 | 目的数据在 **H** 轴的物理跨度 (Stride)。 |
| **Word 5** | `dst_stride_w` | `<191:160>` | 32 | 目的数据在 **W** 轴的物理跨度 (Stride)。 |
| **Word 4** | `dst_stride_c` | `<159:128>` | 32 | 目的数据在 **C** 轴的物理跨度 (Stride)。 |
| **Word 3** | `dma_block_cfg` | `<127:96>` | 32 | **DMA 块维度配置字**：<br>• `[31:16] dst_k`: 目的端 Block 维度 (16 bits)。`0` 代表标准 HWC 格式；`>0` 代表 Blocked 格式，值为具体的 $k$。<br>• `[15:0] src_k`: 源端 Block 维度 (16 bits)。`0` 代表标准 HWC 格式；`>0` 代表 Blocked 格式，值为具体的 $k$。 |
| **Word 2~0** | `Reserved` | `<95:0>` | 96 | **保留字段**。拥有 96 bits 的纯净连续保留空间。 |

---

## 3. 核心设计说明

### 3.1 对齐与无截断解析

由于释放了 64 位地址带来的压力，将 `Shape` 和 `Stride` 统一扩展至 32-bit，并且对齐到 Word 边界。

### 3.2 Layout Transformation

ISA 强制要求分离 `src_stride` 和 `dst_stride`。

### 3.3 Data Type

加入 `data_type` 控制位。由于硬件具有 64 Bytes 的物理位宽限制，DMA AXI 接口在攒够一个 Cache Line 前需要精准知道当前元素的字节数（如 INT8 需 64 个元素打一拍，INT32 仅需 16 个元素打一拍）。
